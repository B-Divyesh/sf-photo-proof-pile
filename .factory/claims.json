[
  {
    "id": "demo-isolated",
    "claim": "The sample review uses separate session-only storage, never changes a real review, and can be reset.",
    "where": "demo banner and README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolated",
    "sandbox": "seed a real local review, open ?demo=1, edit/reset/exit, and compare both storage namespaces"
  },
  {
    "id": "match-evidence",
    "claim": "Shows exact-copy, looks-alike, and same-moment groups with file locations, sizes, dates, cameras, identifiers, and other-drive copies.",
    "where": "review desk and README",
    "test": "npm run test:e2e -- --grep @claim:match-evidence",
    "sandbox": "fresh browser context at /demo; inspect all eight sample rows and every named evidence field"
  },
  {
    "id": "csv-export",
    "claim": "Exports the decision log as CSV.",
    "where": "review toolbar and README",
    "test": "npm run test:e2e -- --grep @claim:csv-export",
    "sandbox": "fresh browser context at /demo; assert the downloaded header and one row for each sample file"
  },
  {
    "id": "reversible-plan",
    "claim": "Keeps recovery records after restart and restores verified decision-log records on the same computer.",
    "where": "landing page, review plan, and README",
    "test": "npm run test:e2e -- --grep @claim:reversible-plan",
    "sandbox": "fresh browser context at /demo; quarantine exact extras, reload, export, clear demo storage, import, and restore"
  },
  {
    "id": "review-before-move",
    "claim": "Only reviewed files marked for quarantine can move, and confirmation names the exact count and destination.",
    "where": "README, review plan, and confirmation",
    "test": "npm run test:claim:review-before-move",
    "sandbox": "native core rejects unreviewed entries and entries without a kept copy; mocked desktop UI proves the disabled state, decision gate, exact confirmation, cancellation, and reviewed native payload"
  },
  {
    "id": "local-privacy",
    "claim": "The browser sample does not upload sample photo data.",
    "where": "demo and privacy page",
    "test": "npm run test:e2e -- --grep @claim:local-privacy",
    "sandbox": "intercept every request during the complete demo review and assert every request is same-origin"
  },
  {
    "id": "no-ad-tracking",
    "claim": "The website does not run advertising or tracking scripts.",
    "where": "privacy page",
    "test": "npm run test:e2e -- --grep @claim:no-ad-tracking",
    "sandbox": "fresh browser context; visit the landing, demo, and privacy routes, inspect loaded scripts, and assert every request is same-origin"
  },
  {
    "id": "native-local-privacy",
    "claim": "Native scanning and quarantine operate on local files without a network client.",
    "where": "privacy page and README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --no-default-features claim_native_local_privacy",
    "sandbox": "temporary local image folders; run scan and quarantine through the native core and assert only local paths change"
  },
  {
    "id": "license-request-privacy",
    "claim": "License verification sends only the license token to Sociobot.",
    "where": "privacy page and README",
    "test": "npm run test:e2e -- --grep @claim:license-request-privacy",
    "sandbox": "seed paths, identifiers, thumbnails, and move records; intercept verification and assert an empty-body token-only GET request"
  },
  {
    "id": "no-account",
    "claim": "Works without an account.",
    "where": "landing page",
    "test": "npm run test:e2e -- --grep @claim:no-account",
    "sandbox": "fresh browser context at /demo; assert the complete review desk opens without identity fields"
  },
  {
    "id": "free-scan-limit",
    "claim": "The free desktop app scans 1,000 image files at a time.",
    "where": "landing page, pricing section, terms, and README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --no-default-features claim_free_scan_limit",
    "sandbox": "temporary folder with 1,001 valid image files; assert exactly 1,000 scanned and a limit flag"
  },
  {
    "id": "free-safety-tools",
    "claim": "The license changes only the scan limit; quarantine and restore remain available without one.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:free-safety-tools",
    "sandbox": "unlicensed mocked desktop session; run two quarantine batches, restart, and restore their recovery records"
  },
  {
    "id": "paid-license",
    "claim": "A license can be restored and is checked at most once each day.",
    "where": "pricing section and README",
    "test": "npm run test:e2e -- --grep @claim:paid-license",
    "sandbox": "mock time and the Sociobot response; paste a token, assert no request at 23:59:59, then one request at 24:00:00"
  },
  {
    "id": "paid-checkout",
    "claim": "A US$29 one-time purchase uses Sociobot hosted checkout and returns a stored license.",
    "where": "landing page, pricing section, terms, and README",
    "test": "npm run test:e2e -- --grep @claim:paid-checkout",
    "sandbox": "fresh browser context; assert the price, external checkout URL, refund email action, returned license storage, and URL cleanup"
  },
  {
    "id": "licensed-scan-limit",
    "claim": "A valid license removes the 1,000-image scan limit.",
    "where": "pricing section, terms, and README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --no-default-features claim_licensed_scan_limit",
    "sandbox": "temporary folder with 1,001 valid images; invoke the licensed native scan and assert all 1,001 are scanned"
  },
  {
    "id": "offline-reload",
    "claim": "The review desk works offline after the first visit.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "load /demo online, wait for its service worker, switch offline, then reload"
  },
  {
    "id": "native-matching",
    "claim": "The desktop scanner creates exact-copy, looks-alike, and same-moment groups, reads capture time and camera, and keeps each file in one group.",
    "where": "README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --no-default-features claim_native_matching",
    "sandbox": "temporary image folder containing exact copies, similar images, and two tagged photos captured in the same minute"
  },
  {
    "id": "scan-scope",
    "claim": "The scanner reads only selected folders and does not change their source files.",
    "where": "README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --no-default-features claim_scan_scope",
    "sandbox": "selected and adjacent unselected image folders; compare reported paths, source bytes, and source modification time"
  },
  {
    "id": "cross-drive-safety",
    "claim": "Cross-drive quarantine copies before removing the source, preserves bytes, embedded photo information, file dates, and avoids name collisions.",
    "where": "README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --no-default-features claim_cross_drive_safety",
    "sandbox": "temporary source and destination folders; exercise copy-before-remove with a tagged image and an occupied quarantine filename"
  },
  {
    "id": "installer-checksum",
    "claim": "The Linux installer matches the package to the published SHA-256 verification file before installing it.",
    "where": "README",
    "test": "npm run test:unit -- --testNamePattern @claim:installer-checksum",
    "sandbox": "mock available and unavailable release metadata plus matching and mismatched package bytes; assert missing or mismatched packages are never installed"
  },
  {
    "id": "windows-installer-checksum",
    "claim": "The Windows installer matches the package to the published SHA-256 verification file before opening it.",
    "where": "README",
    "test": "npm run test:unit -- --testNamePattern @claim:windows-installer-checksum",
    "sandbox": "inspect the shipped PowerShell installer contract; assert package verification happens before msiexec can start"
  },
  {
    "id": "verified-downloads-only",
    "claim": "The download dialog offers desktop packages only after Windows and macOS signature verification is published.",
    "where": "README and download dialog",
    "test": "npm run test:e2e -- --grep @claim:verified-downloads-only",
    "sandbox": "mock a full package matrix without the signed-package marker, then add the marker and assert only the verified release exposes package links"
  }
]
